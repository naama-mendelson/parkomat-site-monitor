# העברת השרת לשרת המשרד

מסמך קצר, ומטרתו נקודה אחת: **המעבר עצמו הוא הרגע המסוכן**, לא ההרצה שאחריו.

## המצב היום

השרת רץ **ידנית על מחשב הפיתוח** (`npm start` ב-`master/`), עם
`MASTER_CLIENT_ID=parkomat-master-subscriber-prod`.

זה עובד, ויש לו כשל אחד ידוע: **הוא לא קם לבד.** ב-10/08 הוא היה כבוי 40 שעות
ואיש לא ידע — הדשבורד הציג נתונים בני יומיים בלי שום סימן שהם ישנים. הנתונים
עצמם לא אבדו (HiveMQ שמר 378 פעולות והן נקלטו בהפעלה מחדש), אבל **הידיעה
אבדה**, וזו הבעיה שהמעבר ל-Docker בא לפתור.

---

## איך בונים את התמונה

⚠️ **לא על מחשב הפיתוח.** הוא מנוהל ארגונית עם Device Guard פעיל, ובתצורה כזו
WSL2 חסום — מנוע ה-Docker אינו יכול לעלות שם כלל. זו מגבלת מדיניות, לא חומרה,
ואי אפשר לעקוף אותה מקומית.

הבנייה רצה ב-GitHub Actions:

1. `Actions` → `build docker image` → `Run workflow`
2. בסיום: `Artifacts` → `parkomat-image` → הורדה

הארטיפקט מכיל שלושה קבצים: `parkomat-image.tar.gz`, `docker-compose.yml`,
`.env.docker.example`.

**התמונה נבנית רק ממה שנדחף ל-GitHub.** עבודה מקומית שלא עברה commit לא תהיה בה.

---

## סדר המעבר — וזה החלק שחייב להיות מדויק

⚠️ **שני שרתים שקולטים בו-זמנית הם המצב שיש להימנע ממנו**, ויש לו שתי צורות
ושתיהן רעות:

| | מה קורה | כמה קל לזהות |
|---|---|---|
| **אותו `MASTER_CLIENT_ID`** | שניהם מנתקים זה את זה בלולאה אינסופית ואף אחד לא קולט | קל — הלוג מלא `reconnecting` |
| **מזהים שונים** | שניהם קולטים בהצלחה וכותבים לאותו מסד במקביל | **קשה מאוד** |

הצורה השנייה היא המסוכנת: היא יוצרת מקטעים כפולים, ארבעה מקטעי מצב פתוחים
בו-זמנית ומשכים שליליים — בדיוק הנזק שתועד כשהקליטה נעשתה מקבילית בעבר.
שום דבר לא נראה שבור עד שמסתכלים על המספרים.

**לכן הסדר הוא:**

1. **לעצור את השרת במחשב הפיתוח.** לוודא שאין תהליך `master.js` חי:
   ```powershell
   Get-CimInstance Win32_Process -Filter "Name='node.exe'" |
     Where-Object { $_.CommandLine -like '*master.js*' }
   ```
2. **בשרת — שתי תמונות, לא אחת:**
   ```bash
   gunzip -c parkomat-image.tar.gz     | docker load
   gunzip -c parkomat-web-image.tar.gz | docker load

   cp .env.docker.example .env      # ולמלא את הערכים האמיתיים
   docker compose up -d
   docker compose logs -f           # לוודא: "REST server running" + subscriber אחד
   ```

   ⚠️ **שני המשתנים האלה חייבים להסכים זה עם זה**, ואם הם לא — הכשל שקט:
   הדף נטען יפה והנתונים לא מגיעים.

   ```
   VITE_API_BASE=http://SERVER-IP:4000       # לאן הדפדפן פונה ל-API
   DASHBOARD_ORIGIN=http://SERVER-IP:8080    # מאיזה origin השרת מקבל (CORS)
   ```

   ⚠️ `VITE_API_BASE` נצרב בזמן **הבנייה** (Vite מחליף אותו בטקסט). שינוי
   שלו מחייב `docker compose build`, ולא `restart`.

   ```
   http://SERVER-IP:8080   ← הדשבורד
   http://SERVER-IP:4000   ← ה-API (לא נפתח בדפדפן)
   ```
3. **רק אחרי שהשרת החדש קולט** — לשנות ב-`master/.env` שבמחשב הפיתוח:
   ```
   MASTER_CLIENT_ID=parkomat-master-subscriber-dev
   ```

⚠️ **שלב 3 אחרון ולא ראשון.** החלפת המזהה לפני המעבר נוטשת session ב-HiveMQ
שממשיך לצבור הודעות שאיש לא קורא, עד שיפוג — צריכת מכסה על ברוקר בחינם, בלי
שום תועלת.

---

## אחרי המעבר — איך ממשיכים לפתח

**הדשבורד קורא את Supabase ישירות** (`VITE_SUPABASE_DIRECT=true`), ולכן:

```bash
cd dashboard && npm run dev
```

עריכה → שמירה → הדפדפן מתעדכן מיד, עם הנתונים החיים מהאתרים, בזמן שהשרת רץ
במשרד. **זה נוח יותר מהיום**, כי היום התצוגה תלויה בכך שהשרת המקומי חי.

⚠️ **מה שאין לעשות: `npm start` מקומי בזמן שהשרת במשרד רץ.** ראה הטבלה למעלה.
אם נדרשת עבודה על ה-API עצמו — לעצור את הקונטיינר בשרת לזמן העבודה, או להוסיף
דגל שמכבה את הקליטה מקומית (טרם נבנה).

---

## פרסום גרסה חדשה

1. commit + push
2. GitHub → Actions → `build docker image` → Run
3. הורדה, העברה לשרת
4. `docker load` + `docker compose up -d` (מרים מחדש עם התמונה החדשה)

⚠️ **`.env` לעולם אינו נוסע עם התמונה.** הוא מכיל את מחרוזת החיבור למסד ואת
פרטי HiveMQ; צריבתם לתמונה הייתה מפיצה אותם לכל מי שמקבל אותה. הוא נשאר בשרת.
