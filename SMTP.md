# חיבור שליחת מיילים

## למה זה חסר היום

Supabase מגיע עם שרת מייל מובנה **לפיתוח בלבד**, מוגבל לכמה מיילים בשעה.
מערכת שנשענת עליו נראית עובדת בבדיקה ונשברת בשקט ביום שמזמינים שלושה
אנשים ברצף, או שמישהו מבקש איפוס פעמיים.

לכן הקוד נבנה סביב ההיעדר: הזמנת משתמש מחזירה **סיסמה זמנית להעתקה**
במקום לשלוח מייל (`email_confirm: true` ב-`auth/admin.js`).

**נמדד מול המופע החי:** בקשת איפוס סיסמה מחזירה `429
over_email_send_rate_limit` על **הבקשה הראשונה**. המיילר המובנה גם שולח
**רק לחברי הפרויקט** — כלומר לרוב המשתמשים המייל לא יגיע לעולם.

---

## ⚠️ ולכן שתי אפשרויות המייל **הוסרו מהמסך**

זו הייתה החלטת מוצר, ולא דחייה: כפתור שמבטיח מייל שלא יגיע גרוע מהיעדרו.
המשתמש ממתין, לוחץ שוב, ואינו יודע שיש בכלל דרך אחרת.

| מה | מצב |
|---|---|
| `sendMagicLink()` — קישור כניסה | **הוסר** מ-`services/auth.js` |
| `requestPasswordReset()` — בקשת איפוס | **הוסר** |
| כפתורי "שלחו לי קישור" / "שכחתי את הסיסמה" | **הוסרו** מ-`Login.jsx` |
| `setNewPassword()` + מסך "קביעת סיסמה חדשה" | ✅ **נשארו** — ראה למטה |
| `onPasswordRecovery()` | ✅ **נשאר** |

⚠️ **למה מסך קביעת הסיסמה נשאר, למרות שאיש כבר לא מבקש קישור:** לוח
הבקרה של Supabase עדיין מציע **Send password recovery** לכל משתמש. קישור
כזה פותח את הדשבורד במצב שחזור — ובלי המסך שמקבל אותו, המשתמש נכנס
מחובר **ובלי שום דרך לקבוע סיסמה**. הסרת הבקשה אינה מצדיקה הסרת הקבלה.

**מסלול ההתאוששות היום:** מנהל מנפיק סיסמה חדשה (מחיקה + הזמנה מחדש, או
`Send password recovery` מלוח הבקרה). כתוב במפורש במסך ההתחברות.

⚠️ **המקרה שאין ממנו חזרה מהמסך:** המנהל **האחרון** ששכח את סיסמתו. אז
צריך את לוח הבקרה של Supabase (מפתח ה-Secret). זה מקובל — אבל כדאי שיהיו
תמיד **שני** מנהלים פעילים, וזו הסיבה שהמערכת מסרבת להשבית או למחוק את
האחרון.

**החזרת הכניסה במייל = כתיבת שתי הפונקציות מחדש.** הבדיקות ב-
`master/tests/read-auth.test.js` מקבעות את ההסרה, ויכשלו בכוונה אם מישהו
יחזיר אחת מהן בלי החלטה מפורשת.

---

## מה להגדיר

**Supabase → Project Settings → Authentication → SMTP Settings → Enable Custom SMTP**

| שדה | מה למלא |
|---|---|
| Sender email | כתובת שולח של החברה, למשל `no-reply@parkomat.co.il` |
| Sender name | `Parkomat SiteMonitor` |
| Host | שרת ה-SMTP של הספק |
| Port | 587 (STARTTLS) או 465 (SSL) |
| Username / Password | מהספק |

### ספק — כולן חינם, וההבדל הוא מה נדרש כדי להתחיל

| | חינם | דורש DNS? | דורש מנהל IT? |
|---|---|---|---|
| **Brevo** ⭐ | 300/יום | לא | **לא** |
| Mailjet | 200/יום | לא | לא |
| Gmail / Microsoft 365 | ~500/יום | לא | **לעיתים** |
| Resend | 3,000/חודש | כן | כן |
| SendGrid | 100/יום | כן | כן |

⚠️ **לשימוש כאן אף אחת מהמכסות אינה רלוונטית** — איפוסי סיסמה והזמנות הם
כמה מיילים **בחודש**, לא ביום.

#### ⚠️ למה Gmail אינו הבחירה הראשונה, למרות שהוא הכי מוכר

הוא דורש **סיסמת אפליקציה**, והדף שמייצר אותה מוסתר בשני מקרים:

  • אין אימות דו-שלבי בחשבון — פתיר בלחיצה.
  • **החשבון הוא Google Workspace ומנהל הארגון חסם סיסמאות אפליקציה** —
    ואת זה אי אפשר לעקוף מהחשבון האישי.

ההודעה זהה בשני המקרים ("ההגדרה שאת מחפשת אינה זמינה בחשבון שלך"), ולכן
אי אפשר לדעת מי מהם — וזה בדיוק מה שהופך את המסלול הזה ללא צפוי.

**נתקלנו בזה בפועל.**

#### Brevo — הדרך שאינה תלויה באיש

1. הרשמה ב-brevo.com
2. **SMTP & API → SMTP** — הפרטים מופיעים מיד:

```
Host      smtp-relay.brevo.com
Port      587
Username  <המייל שנרשמת איתו>
Password  <המפתח שהם נותנים>
```

בלי אימות דומיין, בלי DNS, בלי מנהל IT.

⚠️ **הפשרה:** המייל יגיע מהכתובת שנרשמת איתה ולא מ-`no-reply@parkomat.co.il`.
לשימוש פנימי זה בסדר; לכתובת של החברה צריך Resend + רשומות DNS — עדיין
חינם, אבל דורש את אחראי הדומיין.
---

## ⚠️ ואל תשכחי את זה — בלעדיו הקישורים ישברו

**Supabase → Authentication → URL Configuration**

```
Site URL:        http://<כתובת-הדשבורד>
Redirect URLs:   http://<כתובת-הדשבורד>
                 http://localhost:5173      ← לפיתוח
```

⚠️ **כתובת שאינה ברשימה נדחית בשקט:** הקישור נשלח, המשתמש לוחץ, והוא
מופנה ל-`Site URL` הכללי במקום למסך האיפוס. הוא יתחבר — ולא יקבל שום דרך
לקבוע סיסמה. זה **בדיוק** הכשל שהמסך הזה נבנה למנוע.

היום מוגדר `http://localhost:3000`, ואף אחד לא מאזין שם.

---

## אחרי ההגדרה

⚠️ **המסך לא יציע כלום מעצמו** — הכפתורים הוסרו (למעלה). הבדיקה היא דרך
לוח הבקרה:

1. Supabase → Authentication → Users → בחרו משתמש → **Send password recovery**
2. הקישור מגיע למייל → לחיצה → נפתח מסך "קביעת סיסמה חדשה" בדשבורד
3. אם לא הגיע — Supabase → Logs → Auth, שם תופיע הסיבה

זה מוכיח ש-SMTP עובד **ושמסך השחזור חי**, שתי החוליות שנשארו.

## ורק אחר כך — האם להחזיר את הכפתורים

זו החלטה נפרדת, ולא המשך אוטומטי. שווה לשקול לפי הסדר:

1. **"שכחתי את הסיסמה"** — הרווח הגדול ביותר: מסיר את התלות במנהל.
2. **ההזמנה שולחת מייל** במקום סיסמה זמנית להעתקה. היום זה מעקף של
   ההיעדר, לא בחירה.
3. **קישור כניסה (magic link)** — הכי פחות דחוף, ומוסיף מסלול כניסה שני
   שצריך להחזיק ולבדוק.

⚠️ **ואל תשכחו למחוק את הבדיקות שמקבעות את ההסרה** (`read-auth.test.js`),
אחרת הן ייכשלו — הן שם בדיוק כדי שההחזרה תהיה החלטה ולא תאונה.
