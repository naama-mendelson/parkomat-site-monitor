# תוכנית ב' — היום שבו נרצה להתנתק מ-Supabase

מסמך הפעלה. לא הסבר על למה, אלא **מה לעשות, לפי הסדר, וכמה זה עולה.**

הכלל שמאחורי הכול: כל תלות ב-Supabase נמצאת מאחורי מתג או מאחורי seam, ואף
אחת מהן אינה פזורה בקוד. לכן היציאה היא רצף החלטות ולא פרויקט גילוי.

---

## המתג הראשי — מה שנשאל עליו

```
dashboard/.env
VITE_SUPABASE_DIRECT=true     ← היום. הדשבורד קורא ישירות מ-Supabase
VITE_SUPABASE_DIRECT=false    ← הכול חוזר לעבור דרך השרת
```

`false` + `npm run build` — וזהו. אין שינוי קוד, אין הגירת נתונים, אין השבתה.

**מה זה עושה בפועל:** `services/dataSource.js` בוחר בין `fetchSitesDirect`
(PostgREST) לבין `fetchSites` (REST של השרת). שתי הזרועות מחזירות **אותו מבנה
בדיוק**, ולכן אף קומפוננטה אינה יודעת מי ענה.

**נבדק, לא משוער:** שני המצבים נטענים בדפדפן ומציגים 12 כרטיסים **זהים תו
בתו**, כולל אחוזי זמינות ואחוזי כשל. המצב הישיר פונה ל-PostgREST ולא נוגע
ב-`/api/sites`; מצב השרת פונה ל-`/api/sites` ולא נוגע ב-PostgREST.

---

## ⚠️ ההשלכה שצריך להסכים עליה מראש

התוכנית המקורית כללה שלב E: **למחוק את 17 נתיבי הקריאה בשרת** אחרי שהדשבורד
עובר לקריאה ישירה.

**שלב E סותר את המסמך הזה.** הנתיבים האלה *הם* דרך החזרה. אי אפשר גם למחוק
אותם וגם להחזיק מסלול נסיגה.

ההכרעה שננקטה: **הנתיבים נשארים.** השרת מצטמצם בכך שהוא אינו בשימוש, לא בכך
שנמחק ממנו קוד.

| | מחיקה (שלב E) | שמירה (מה שנעשה) |
|---|---|---|
| שורות בשרת | ~2,200 | 8,661 |
| חזרה מ-Supabase | פרויקט הגירה | שינוי משתנה אחד |
| קוד שאינו רץ | אין | 17 נתיבים |

אם בעתיד יוחלט לוותר על תוכנית ב' — המחיקה היא צעד נפרד ומודע, ולא תופעת
לוואי של ניקיון.

**מסלול שאינו רץ נרקב.** לכן שני המצבים נבדקים בדפדפן לפני כל שחרור, באותה
לוגיקה שבגללה האימות העצמאי הרדום מכוסה בטסטים.

---

## ארבע התלויות, וכמה עולה לנתק כל אחת

### 1. חישוב המדדים — עלות אפס

הפונקציות (`site_uptime`, `site_stats`, `site_segments_collapsed`,
`site_globals`) הן **Postgres תקני**. אין בהן שום דבר של Supabase — זה נאכף
ככלל: *אין `auth.*` בתוך פונקציית מדד*.

הן נוסעות ב-`pg_dump` ורצות על כל Postgres 15+. `npm run parity` מאמת אותן מול
ה-JS: 1,262 השוואות, 0 הבדלים.

וה-JS **לא נמחק** — `loadRangeData`, `uptimeFromData`, `statsFromData` ו-
`getAllSitesGlobals` חיים ומשמשים את supervisor/executive/analytics. כלומר גם
אם ה-SQL היה נעלם, החישוב קיים בשתי שפות.

### 2. גישה ישירה — עלות נמוכה

**PostgREST הוא תוכנה עומדת בפני עצמה** ורצה בהתקנה עצמית. זו הסיבה שהוא נבחר
כשכבת הגישה מלכתחילה, ולא ה-SDK הקנייני.

`supabase-js` מיובא **בקובץ אחד בלבד** (`services/supabase.js`) — זה כלל
בדוק, לא שאיפה. החלפה = קובץ אחד.

או פשוט: `VITE_SUPABASE_DIRECT=false`, ואז שכבת הגישה כולה אינה בשימוש.

### 3. הנתונים — עלות בינונית

```sh
pg_dump --schema=public --schema=app "$DATABASE_URL" > parkomat.sql
```

⚠️ **`--schema=app` הוא חובה.** הסכמה `app` מחזיקה את `current_actor`,
`current_role` ו-`allowed_email_domains`. `pg_dump --schema=public` לבדו — הצורה
שכולם מעתיקים — משאיר אותן מאחור, וההשבה תיכשל על פונקציות חסרות.

**אין אף מפתח זר ל-`auth.users`.** זה כלל אכוף בפרויקט, והוא הסיבה שה-dump
עומד בפני עצמו. הדפוס שכל מדריך של Supabase מלמד היה הופך את היציאה לבלתי
אפשרית בלי בנייה מחדש של גרף המשתמשים.

### 4. האימות — העלות האמיתית

```
master/.env
AUTH_PROVIDER=supabase      ← היום
AUTH_PROVIDER=selfhosted    ← המסלול הרדום
```

ה-seam קיים ומכוסה: **27 טסטים רצים על שני הספקים**, כדי שהרדום לא ירקב.

**אבל כאן נמצא המחיר, ואין דרך לעקוף אותו:**

- אימות האסימון בנוי ובדוק בשני הספקים.
- **הנפקת אסימון בהתקנה עצמית אינה בנויה** — אין טבלת משתמשים, אין גיבוב
  סיסמאות, אין נתיב התחברות. זה מכוון: הם נבנו אחרי שיהיו משתמשים אמיתיים,
  ולא נגד ניחוש.
- **כל המשתמשים ירשמו מחדש.** סיסמאות מגובבות ב-GoTrue ואינן ניתנות להעברה
  שימושית. כל אחד יקבל סיסמה זמנית חדשה, כמו בהזמנה.
- **הגבלת הדומיין תזדקק לקישור מחדש.** הלוגיקה ב-`app` נוסעת ב-dump; הטריגר
  יושב על `auth.users` ואינו נוסע. יצירת טריגר אחד מחדש על טבלת המשתמשים
  החדשה.

**זו העלות האמיתית של היציאה.** כל השאר הוא משתנה סביבה.

---

## סדר הפעולות ביום היציאה

1. **`VITE_SUPABASE_DIRECT=false`** ובנייה. הדשבורד מפסיק לגעת ב-Supabase.
   *מכאן והלאה אין דחיפות* — המערכת עובדת דרך השרת, ו-Supabase הוא רק בסיס
   נתונים.
2. `pg_dump --schema=public --schema=app`, השבה על Postgres 15+.
3. `DATABASE_URL` בשרת → המופע החדש. הקליטה ממשיכה.
4. `npm run parity` על המופע החדש — לוודא שהמספרים לא זזו.
5. בניית הנפקת האסימון ב-`selfhosted`, ורישום מחדש של המשתמשים.
6. `AUTH_PROVIDER=selfhosted`.

השלבים 1–4 הפיכים לחלוטין. רק שלב 5 הוא עבודה שאין ממנה חזרה.

---

## מה **לא** משתנה ביום היציאה

הסוכן באתרים, HiveMQ, וכל לוגיקת הקליטה — dedup, שער סבירות, סדר LWT, כללי
מונה המחזורים. שום דבר מזה לא נגע ב-Supabase מעולם, ולכן שום דבר מזה אינו
חלק מהיציאה.

---

## מה שעדיין תלוי בשרת גם במצב הישיר

המתג מנתב את **קריאת האתרים**. עדיין עוברים דרך השרת:

- **SSE** (`/api/stream`) — עדכונים חיים. טבלת `events` היא חוזה האירועים, ו-
  Realtime של Supabase יכולה לקרוא ממנה, אבל היום הקורא הוא SSE.
- **עוזר ה-AI** (`/api/chat`) — מחזיק `GROQ_API_KEY`, שאסור שיגיע לדפדפן.
- **קליטת MQTT** — דורשת תהליך שנשאר מחובר. אין לזה מקבילה serverless.
- **supervisor / executive / analytics / insights** — עדיין מחשבים ב-JS.

הראשון והאחרון הם עבודה עתידית. השניים באמצע **לעולם לא יזוזו**, וזה בסדר:
תוכנית ב' לא נועדה לבטל את השרת אלא לוודא שהוא מספיק.

---

## הטריגר הצפוי

**עלות.** בסדר הגודל הנוכחי (12 אתרים) המצב יציב סביב 47–82MB — לא נושא. ב-200 אתרים הגבול
נחצה תוך 6–12 חודשים. נקודת המפנה היא בערך 60–80 אתרים.

`DELETE` אינו מחזיר שטח דיסק, ולכן שמירת הנתונים מתייצבת על השיא ההיסטורי ולא
יורדת. בניגוד לרוב טריגרי ההגירה, את זה **רואים מראש**.
