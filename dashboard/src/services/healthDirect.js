// services/healthDirect.js — האם השרת חי, לפי אות החיים שהוא כותב על עצמו.
//
// ============================================================
// למה זה קיים, ומה בדיוק נשבר בלעדיו
// ============================================================
// ב-26.07 השרת היה למטה 15 שעות, וב-22.08 עוד 14.7. בשני המקרים **לא אבד
// נתון אחד** — HiveMQ שומר את התור ומוסר אותו בהפעלה הבאה — אבל הדשבורד
// הראה מצב ישן **בלי שום סימן שהוא ישן**. אין שגיאה לקרוא, אין כרטיס אדום;
// המסך פשוט קפוא ונראה תקין.
//
// ⚠️ וזה חמור דווקא **בגלל** שהמעבר ל-Supabase הצליח: הדשבורד קורא ישירות
// מ-PostgREST, ולכן הוא ממשיך לעבוד מצוין בזמן שהקליטה מתה. כל המסכים
// נטענים, כל המספרים מוצגים — הם פשוט מאתמול.
//
// ============================================================
// ⚠️ למה אות חיים ולא גיל הנתונים
// ============================================================
// המדד המתבקש הוא "מתי התקבלה ההודעה האחרונה". הוא אינו עובד: הסוכן
// משדר רק ב**שינוי** MODE, ולכן חניון שקט בלילה מייצר אפס הודעות — בדיוק
// כמו שרת מת. גיל הנתונים אינו יכול להבחין ביניהם, ומסך שיצעק על כל שעה
// שקטה הוא מסך שמלמדים להתעלם ממנו תוך שבוע.
//
// השרת כותב שורה על עצמו כל 20 שניות (ה-keep-alive ב-master.js). שורה
// שהפסיקה להתעדכן היא עובדה על השרת, לא ניחוש מהיעדר תנועה.
import { supabase, isSupabaseConfigured } from "./supabase";

/**
 * מחזיר `{ alive, ageSeconds, lastBeatAt, unknown }`.
 *
 * ⚠️ `unknown` נבדל מ-`alive:false` בכוונה. אין אות חיים בכלל פירושו שרת
 * שטרם נפרס עם התכונה — ולהכריז עליו "מת" היה מציג אזהרה אדומה על מערכת
 * תקינה לגמרי, כלומר בדיוק סוג ההתראה שגורם לאנשים להפסיק להאמין למסך.
 */
export async function fetchServerHealthDirect(staleAfterSeconds = 300) {
  if (!isSupabaseConfigured) throw new Error("Supabase אינו מוגדר בדשבורד");

  const { data, error } = await supabase.rpc("server_heartbeat");
  if (error) throw new Error(error.message || "בדיקת אות החיים נכשלה");

  if (!data) return { alive: null, ageSeconds: null, lastBeatAt: null, unknown: true };

  const beat = new Date(data).getTime();
  if (Number.isNaN(beat)) {
    return { alive: null, ageSeconds: null, lastBeatAt: null, unknown: true };
  }

  // ⚠️ שעון הדפדפן מול שעון השרת: פער קטן וחיובי הוא נורמלי ואינו מעניין,
  // אבל שעון מקומי שמקדים היה מייצר גיל שלילי ואז "חי" לנצח. Math.max
  // מקבע את הרצפה באפס.
  const ageSeconds = Math.max(0, Math.round((Date.now() - beat) / 1000));
  return {
    alive: ageSeconds < staleAfterSeconds,
    ageSeconds,
    lastBeatAt: data,
    unknown: false,
  };
}
