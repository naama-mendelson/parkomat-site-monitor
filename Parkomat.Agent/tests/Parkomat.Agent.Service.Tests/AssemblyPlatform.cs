// ⚠️ אותה הצהרה כמו בשירות, ומאותה סיבה. בלעדיה הכיוון מתהפך: ברגע
// שהשירות מוצהר Windows, **כל קריאה אליו** מפרויקט שאינו מוצהר נדלקת —
// נמדד: 18 אזהרות הפכו ל-708.
//
// הבדיקות האלה קוראות את הרישום, את מתזמן המשימות ואת קוד ה-Tray, כלומר
// הן ממילא Windows בלבד. הצהרה שמתארת את המציאות מחזירה את רשימת
// האזהרות למצב שבו אפשר לקרוא אותה.
using System.Runtime.Versioning;

[assembly: SupportedOSPlatform("windows")]
