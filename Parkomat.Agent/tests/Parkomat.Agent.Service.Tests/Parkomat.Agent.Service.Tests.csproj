<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="coverlet.collector" Version="6.0.4" />
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.14.1" />
    <PackageReference Include="xunit" Version="2.9.3" />
    <PackageReference Include="xunit.runner.visualstudio" Version="3.1.4" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Xunit" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\Parkomat.Agent.Service\Parkomat.Agent.Service.csproj" />
  </ItemGroup>

  <!-- ============================================================
       קובץ אחד מה-Tray, מקושר ולא מועתק
       ============================================================
       ⚠️ פרויקט ה-Tray הוא net10.0-windows ו-WinForms, ולכן הפניה אליו
       מכאן אינה אפשרית. אבל `KeepAliveTask` הוא הקוד שרושם את המשימה
       שמחזירה אתר לחיים, והדרך היחידה האחרת לבדוק אותו היא להתקין
       באתר — כלומר לגלות תקלה על 21 מכונות.

       ⚠️ **מקושר ולא מועתק**: עותק היה נשאר ירוק בזמן שהמקור השתנה,
       וזו בדיוק הבדיקה שגרועה מכלום. אם הקובץ יזוז — הבנייה נשברת,
       וזה הכיוון הנכון. -->
  <ItemGroup>
    <Compile Include="..\..\src\Parkomat.Agent.Tray\Services\KeepAliveTask.cs"
             Link="Linked\KeepAliveTask.cs" />

    <!-- ⚠️ ו-`ConnectionTester` מאותה סיבה בדיוק. הוא בדק TCP ו-FC04
         תמיד, גם באתר שמוגדר UDP או 0x03 — כלומר שיקר דווקא באתרים
         שבשבילם התכונות נבנו. בלי קישור, הדרך היחידה לבדוק אותו היא
         לפתוח את המסך בעיניים באתר. -->
    <Compile Include="..\..\src\Parkomat.Agent.Tray\Services\ConnectionTester.cs"
             Link="Linked\ConnectionTester.cs" />
  </ItemGroup>

</Project>