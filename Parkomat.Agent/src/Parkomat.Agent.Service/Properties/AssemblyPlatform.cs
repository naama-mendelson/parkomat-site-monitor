// ============================================================
// ⚠️ השירות הוא Windows בלבד — הצהרה, לא השתקה
// ============================================================
// הוא נרשם כשירות Windows, קורא את הרישום (AutoStartProbe), מריץ את
// Mosquitto ומדבר עם w32time. בלי ההצהרה הזו המנתח הניח "רץ בכל
// פלטפורמה" והדפיס 18 אזהרות CA1416 בכל בנייה.
//
// ⚠️ **והמחיר של אזהרה קבועה אינו רעש אלא עיוורון.** רשימה שמכילה 18
// אזהרות ידועות היא רשימה שאיש אינו קורא, וביום שתופיע בה אזהרה
// אמיתית — היא תיבלע בדיוק כמו השאר. זו הסיבה לתקן ולא לכבות את הכלל.
//
// ⚠️ וההצהרה **אינה** משתיקה את הבדיקה: היא מספרת למנתח מה נכון. קריאה
// ל-API שאינו נתמך ב-Windows עדיין תידלק, וזה בדיוק מה שרוצים ממנה.
//
// ⚠️ ולמה לא TargetFramework=net10.0-windows: זה היה משנה את צורת
// הפרסום ואת ה-RID של קובץ ההתקנה — שינוי אריזה כדי לפתור אזהרת ניתוח.
using System.Runtime.Versioning;

[assembly: SupportedOSPlatform("windows")]
